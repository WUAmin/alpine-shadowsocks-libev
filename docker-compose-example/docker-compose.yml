shadowsocks-1:
  image: wuamin/alpine-shadowsocks-libev
  container_name: shadowsocks-1
  ports:
    - "${PORT}:${SRV_PORT}"
  environment:
    - SRV_PORT=
    - METHOD=
    - PASSWORD=
    - DNS_ADDR=
    - DNS_ADDR_2=
  restart: always